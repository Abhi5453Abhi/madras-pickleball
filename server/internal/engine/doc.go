// Package engine is the pure heart of the app: draws, standings and
// tiebreaks, the scoring rules, the finish-time estimate, and the parsing of
// a pasted player list. No database, no clock, no HTTP — every function
// takes values and returns values, and is tested that way.
package engine
